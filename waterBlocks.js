const findValley = (towers, place) => {
  // expand out searching for higher points on either side
  // return true if we are the lowest point and false if notZ
};

const waterBlocks = towers => {
  // find lowest number that has edges
  // return 1 + list with that number incremented by 1
};
