function diagonalDifference(a) {
  // Complete this function
  const n = matrix[(0, 0)];
  const sumMajorDiagonal = matrix => {
    let sum = 0;

    return sum;
  };
  const sumMinorDiagonal = matrix => {
    let sum = 0;
    return sum;
  };
  const diff = Math.abs(sumMajorDiagonal(a) - sumMinorDiagonal(a));
  return diff;
}
